export default class Stage {
    constructor(Controller) {
        this.context = Controller;
    }
    setScore() {
        this.context.setScore(false);
    }
    this.context.setCurrentCard(card);
}